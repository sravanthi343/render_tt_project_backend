spring.application.name=complaint-management-system

# Server port - use PORT env var for Render, fallback to 8080
server.port=${PORT:8080}

# PostgreSQL Database
#spring.datasource.url=${DATABASE_URL}
spring.datasource.driver-class-name=org.postgresql.Driver
#spring.datasource.username=${DB_USERNAME}
#spring.datasource.password=${DB_PASSWORD}
SPRING_DATASOURCE_URL=jdbc:postgresql://dpg-d6qj4015pdvs73bbe3bg-a:5432/sravanthi_168d
SPRING_DATASOURCE_USERNAME=sravanthi
SPRING_DATASOURCE_PASSWORD=lvNbdWsmYgrA9D3DDS55ZWYu7PlGVJcL

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# JWT
jwt.secret=${JWT_SECRET:cms-jwt-secret-key-college-management-2024-secure-render}
jwt.expiration=${JWT_EXPIRATION:86400000}
